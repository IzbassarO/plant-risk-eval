% ICA 2026 -- два производных значения, которых генератор пока не эмитит.
%
% Оба вычислены арифметикой из чисел, уже лежащих в results_macros.tex, а не
% измерены заново:
%
%   \EceRatio<m>  = mean_ece[m]["xd_T"] / mean_ece[m]["xd"]
%   \TempSdMax    = max(sd_of_fitted_T[m] for m in backbones)   # PlantVillage
%
% \providecommand: как только генератор начнёт их выдавать, его определения
% (загружаются первыми) выиграют, и этот файл станет no-op.

\providecommand{\EceRatioRn}{1.48}
\providecommand{\EceRatioEb}{1.55}
\providecommand{\EceRatioMn}{1.94}
\providecommand{\TempSdMax}{0.0076}