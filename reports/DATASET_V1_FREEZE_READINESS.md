# Dataset V1 — freeze readiness

_Generated by `scripts/build_dataset_v1_freeze_readiness.py`. **This assessment freezes nothing.** It writes no freeze artifact, approves nothing, and changes no record. `ready` means only that every precondition below is satisfied and a human may then consider the decision._

**Status: `NOT_READY`** — 9 satisfied, 6 blocked.
**Dataset V1 frozen: NO.**

## Blocking conditions

- **Every disease->action mapping row carries a terminal human decision** (`disease_action_mapping_reviewed`, human) — 17 of 28 row(s) still needs_review
- **Every PlantVillage class has an action mapping** (`plantvillage_action_mapping_coverage`, human) — coverage 0/38 PlantVillage class(es)
- **A human-approved harm matrix exists** (`harm_matrix_approved`, human) — matrix 'harm_v0_template' review_status='pending', is_example=False
- **Open audit findings are closed by an independent re-audit** (`open_audit_findings_closed`, human) — no sign-off artifact; ['AUD-EC-005', 'AUD-EC-006', 'AUD-EC-007'] remain open
- **The R2B duplicate remediation has itself been independently audited** (`remediation_independently_audited`, human) — not recorded in any sign-off artifact
- **A human has recorded an explicit Dataset V1 freeze approval** (`freeze_approval_recorded`, human) — no approval artifact; the freeze decision has not been taken

## All conditions

| Condition | Kind | Status | Detail |
|---|---|---|---|
| PlantDoc is acquired to the frozen upstream snapshot (`plantdoc_acquired`) | machine | satisfied | complete=True; manifest 2578 of upstream 2578 |
| PlantVillage pixels are materialized (`plantvillage_materialized`) | machine | satisfied | pixels_materialized=True; 54305 image(s) |
| Cross-dataset leakage gate passes (`cross_dataset_leakage_gate`) | machine | satisfied | schema 2.1, exact 0, near 16 all resolved, 0 unresolved, 0 violations, every bound input digest current |
| PlantDoc internal duplicate gate passes (`internal_duplicate_gate`) | machine | satisfied | schema 2.0, 12/12 groups adjudicated and applied, 10 retained, 14 excluded, fresh==persisted |
| The effective dataset matches a fresh rebuild from the decisions (`effective_dataset_current`) | machine | satisfied | fresh 2564 == persisted 2564, identity digest 2c258bc766bef7da…, byte-identical |
| The effective dataset is internally sound (`effective_dataset_integrity`) | machine | satisfied | 2564 record(s), 2564 distinct digest(s), 0 exact duplicates, unique relpaths and identities, 28 class(es) |
| Every effective record's bytes match its recorded digest (`effective_dataset_pixels_verified`) | machine | satisfied | all 2564 record(s) re-hashed from disk and matching |
| Preserved human-review evidence is unaltered (`human_review_evidence_intact`) | machine | satisfied | 8 preserved artifact(s), all digests current |
| No machine-specific or personal identifier in paper artifacts (`anonymity`) | machine | satisfied | 0 error(s), 0 warning(s) |
| Every disease->action mapping row carries a terminal human decision (`disease_action_mapping_reviewed`) | human | **BLOCKED** | 17 of 28 row(s) still needs_review |
| Every PlantVillage class has an action mapping (`plantvillage_action_mapping_coverage`) | human | **BLOCKED** | coverage 0/38 PlantVillage class(es) |
| A human-approved harm matrix exists (`harm_matrix_approved`) | human | **BLOCKED** | matrix 'harm_v0_template' review_status='pending', is_example=False |
| Open audit findings are closed by an independent re-audit (`open_audit_findings_closed`) | human | **BLOCKED** | no sign-off artifact; ['AUD-EC-005', 'AUD-EC-006', 'AUD-EC-007'] remain open |
| The R2B duplicate remediation has itself been independently audited (`remediation_independently_audited`) | human | **BLOCKED** | not recorded in any sign-off artifact |
| A human has recorded an explicit Dataset V1 freeze approval (`freeze_approval_recorded`) | human | **BLOCKED** | no approval artifact; the freeze decision has not been taken |

## What a human must produce

A `machine` condition is evaluated here from artifacts, digests, and a fresh rebuild. A `human` condition is a scientific judgement and is satisfied **only** by an explicit decision artifact — its absence is a blocker, never a default.

| Artifact | Satisfies |
|---|---|
| `reports/DATASET_V1_AUDIT_SIGNOFF.json` | `open_audit_findings_closed` — JSON with `closed_findings` listing at least ['AUD-EC-005', 'AUD-EC-006', 'AUD-EC-007']; and `r2b_remediation_independently_audited: true` for `remediation_independently_audited` |
| `data/manifests/dataset_v1_freeze_approval.json` | `freeze_approval_recorded` — JSON with `approved: true`, an anonymous reviewer id, and an ISO-8601 timestamp with offset |
| `data/mapping/action_mapping_review.csv` | the two mapping conditions, once every row carries a terminal decision and PlantVillage coverage is complete |
| `configs/harm_matrix_template.yaml` | `harm_matrix_approved`, once a non-example matrix is approved with complete weights |

## Re-deriving this assessment

```
python scripts/build_dataset_v1_freeze_readiness.py
python scripts/build_dataset_v1_freeze_readiness.py --check
```

No wall-clock field, so repeated runs are byte-identical and the assessment can be compared across commits.
