# Dataset V1 — freeze readiness

_Generated by `scripts/build_dataset_v1_freeze_readiness.py`. **This assessment freezes nothing.** It writes no freeze artifact, approves nothing, and changes no record. `ready` means only that every precondition below is satisfied and a human may then consider the decision._

**Status: `NOT_READY`** — 9 satisfied, 6 blocked.
**Dataset V1 frozen: NO.**

## Blocking conditions

- **Every PlantDoc class carries a terminal, evidence-gated mapping decision** (`disease_action_mapping_reviewed`, human_scientific) — coverage 11/28; 17 non-terminal: ["Apple Scab Leaf: status 'needs_review' is not terminal (terminal: ['approved', 'excluded'])", "Apple rust leaf: status 'needs_review' is not terminal (terminal: ['approved', 'excluded'])", "Bell_pepper leaf spot: status 'needs_review' is not terminal (terminal: ['approved', 'excluded'])"] (+14 more)
- **Every PlantVillage class carries a terminal, evidence-gated mapping decision** (`plantvillage_action_mapping_coverage`, human_scientific) — coverage 0/38; 38 missing: ['Apple___Apple_scab', 'Apple___Black_rot', 'Apple___Cedar_apple_rust'] (+35 more)
- **A human-approved harm matrix exists** (`harm_matrix_approved`, human_scientific) — matrix 'harm_v0_template' review_status='pending', is_example=False
- **The canonical relabels ['G07', 'G08', 'G10'] carry an independent second review** (`relabel_second_scientific_review`, human_scientific) — no plantdoc_label_second_review artifact at human_review/plantdoc_label_second_review/second_review.json; the decision has not been taken; packet pending at reports/plantdoc_label_second_review
- **An independent re-audit closes ['AUD-EC-005', 'AUD-EC-006', 'AUD-EC-007', 'R2B-REMEDIATION']** (`open_audit_findings_closed`, independent_audit) — no dataset_v1_audit_signoff artifact at reports/DATASET_V1_AUDIT_SIGNOFF.json; the decision has not been taken
- **A human has recorded a valid, bound Dataset V1 freeze approval** (`freeze_approval_recorded`, governance_approval) — no dataset_v1_freeze_approval artifact at data/manifests/dataset_v1_freeze_approval.json; the decision has not been taken

## All conditions

| Condition | Kind | Status | Detail |
|---|---|---|---|
| PlantDoc is acquired to the frozen upstream snapshot (`plantdoc_acquired`) | machine | satisfied | complete=True; manifest 2578 of upstream 2578 |
| PlantVillage pixels are materialized (`plantvillage_materialized`) | machine | satisfied | pixels_materialized=True; 54305 image(s) |
| Cross-dataset leakage gate passes (`cross_dataset_leakage_gate`) | machine | satisfied | schema 2.1, exact 0, near 16 all resolved, 0 unresolved, 0 violations, every bound input digest current |
| PlantDoc internal duplicate gate passes (`internal_duplicate_gate`) | machine | satisfied | schema 2.0, 12/12 groups adjudicated and applied, 10 retained, 14 excluded, fresh==persisted |
| The effective dataset matches a fresh rebuild from the decisions (`effective_dataset_current`) | machine | satisfied | fresh 2564 == persisted 2564, identity digest 2c258bc766bef7da…, byte-identical |
| The effective dataset is internally sound (`effective_dataset_integrity`) | machine | satisfied | 2564 record(s), 2564 distinct digest(s), 0 exact duplicates, unique relpaths and identities, 28 class(es) |
| Every effective record's bytes match its recorded digest (`effective_dataset_pixels_verified`) | machine | satisfied | all 2564 record(s) re-hashed from disk and matching |
| Preserved human-review evidence is unaltered (`human_review_evidence_intact`) | machine | satisfied | 8 preserved artifact(s), all digests current |
| No machine-specific or personal identifier in paper artifacts (`anonymity`) | machine | satisfied | 0 error(s), 0 warning(s) |
| Every PlantDoc class carries a terminal, evidence-gated mapping decision (`disease_action_mapping_reviewed`) | human_scientific | **BLOCKED** | coverage 11/28; 17 non-terminal: ["Apple Scab Leaf: status 'needs_review' is not terminal (terminal: ['approved', 'excluded'])", "Apple rust leaf: status 'needs_review' is not terminal (terminal: ['approved', 'excluded'])", "Bell_pepper leaf spot: status 'needs_review' is not terminal (terminal: ['approved', 'excluded'])"] (+14 more) |
| Every PlantVillage class carries a terminal, evidence-gated mapping decision (`plantvillage_action_mapping_coverage`) | human_scientific | **BLOCKED** | coverage 0/38; 38 missing: ['Apple___Apple_scab', 'Apple___Black_rot', 'Apple___Cedar_apple_rust'] (+35 more) |
| A human-approved harm matrix exists (`harm_matrix_approved`) | human_scientific | **BLOCKED** | matrix 'harm_v0_template' review_status='pending', is_example=False |
| The canonical relabels ['G07', 'G08', 'G10'] carry an independent second review (`relabel_second_scientific_review`) | human_scientific | **BLOCKED** | no plantdoc_label_second_review artifact at human_review/plantdoc_label_second_review/second_review.json; the decision has not been taken; packet pending at reports/plantdoc_label_second_review |
| An independent re-audit closes ['AUD-EC-005', 'AUD-EC-006', 'AUD-EC-007', 'R2B-REMEDIATION'] (`open_audit_findings_closed`) | independent_audit | **BLOCKED** | no dataset_v1_audit_signoff artifact at reports/DATASET_V1_AUDIT_SIGNOFF.json; the decision has not been taken |
| A human has recorded a valid, bound Dataset V1 freeze approval (`freeze_approval_recorded`) | governance_approval | **BLOCKED** | no dataset_v1_freeze_approval artifact at data/manifests/dataset_v1_freeze_approval.json; the decision has not been taken |

## Who can settle what

A `machine` condition is evaluated here from artifacts, bound digests, and a fresh rebuild. The other three are decisions, and they are **not** interchangeable — a governance approval cannot stand in for a scientific judgement, and neither can stand in for an independent audit. Each is satisfied only by an explicit artifact validated against a versioned schema (`ica26.governance.approval/1`); absence, invalidity, or a stale binding all block.

| Kind | Meaning |
|---|---|
| `machine` | The pipeline can verify it. |
| `human_scientific` | A domain judgement about the data itself. |
| `governance_approval` | A decision to proceed, taken by an accountable owner. |
| `independent_audit` | A verdict by someone who did not do the work. |

## What a human must produce

| Artifact | Satisfies | Must carry |
|---|---|---|
| `reports/DATASET_V1_AUDIT_SIGNOFF.json` | `open_audit_findings_closed` | schema version, artifact type `dataset_v1_audit_signoff`, scope `phase1_audit_findings`, decision, reviewer id **and role**, ISO-8601 timestamp with offset, the repository commit, SHA-256 bindings for the gate and effective manifest, a rationale, `closed_findings` covering ['AUD-EC-005', 'AUD-EC-006', 'AUD-EC-007', 'R2B-REMEDIATION'], `auditor_independent_of_implementer: true`, and an explicit `not_approved` |
| `human_review/plantdoc_label_second_review/second_review.json` | `relabel_second_scientific_review` | the same core fields with artifact type `plantdoc_label_second_review`, plus `group_verdicts` and `diagnostic_citations`. The pending packet is at `reports/plantdoc_label_second_review` |
| `data/manifests/dataset_v1_freeze_approval.json` | `freeze_approval_recorded` | the same core fields with artifact type `dataset_v1_freeze_approval` and scope `dataset_v1`, binding the effective manifest, resolution table, and both gates |
| `data/mapping/action_mapping_review.csv` | both mapping conditions | one row per dataset class, each with a terminal status (`approved` or `excluded`), the full evidence set, a target from the approved action vocabulary, and reviewer attribution |
| `configs/harm_matrix_template.yaml` | `harm_matrix_approved` | a non-example matrix, approved, with complete weights |

An approval is refused for: a missing or placeholder field, an unknown decision value, an invalid or naive timestamp, an empty reviewer, a commit that is not HEAD, a digest that no longer matches the file it binds, the wrong artifact type or scope, a duplicate approval for the same scope, or a timestamp predating the commit it claims to have reviewed. A bare `{"approved": true}` fails on all counts and is pinned as a test.

## Re-deriving this assessment

```
python scripts/build_dataset_v1_freeze_readiness.py
python scripts/build_dataset_v1_freeze_readiness.py --check
```

No wall-clock field, so repeated runs are byte-identical and the assessment can be compared across commits. Every condition records the SHA-256 of the artifact it was judged from, so a verdict cannot outlive its evidence.
