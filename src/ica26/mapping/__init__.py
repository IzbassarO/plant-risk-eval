"""Disease-to-action mapping: schema, deterministic crosswalk, and validation.

Submodules: ``schema``, ``crosswalk``, ``validation``. Import directly, e.g.
``from ica26.mapping import validation``.
"""
from __future__ import annotations

__all__ = ["crosswalk", "schema", "validation"]
