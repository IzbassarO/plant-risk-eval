# ICA-2026 Phase-1 core dependencies.
# The package itself is defined in pyproject.toml; install it with:
#     pip install -e .
# These bounded versions install cleanly on Colab free tier and locally.

numpy>=1.24,<3
pandas>=2.0,<4
pillow>=10,<12
imagehash>=4.3,<5
pyyaml>=6,<7
requests>=2.28,<3

# dev / tests
pytest>=8,<9

# ---------------------------------------------------------------------------
# Optional extras (install only when needed; kept out of the core to stay light)
#
# PlantVillage acquisition from Hugging Face (preserves leaf_id):
#     pip install "ica26[hf]"     # -> datasets>=2.19, huggingface_hub>=0.23
#
# Notebook EDA / later-phase modelling (heavier; Colab):
#     matplotlib>=3.7,<4
#     scikit-learn>=1.3,<2
#     scikit-image>=0.21,<1
#     torch ; torchvision            # GPU baseline, Colab only
# ---------------------------------------------------------------------------
